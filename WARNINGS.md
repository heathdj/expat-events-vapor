# Build warnings tracker

Every warning `swift build` has produced against this codebase, so nothing gets silently carried to launch. Each entry has a status: **fixed**, **suppressed** (deliberately silenced, with a reason), or **open** (still needs a decision). Update this file whenever a build log shows a new warning or an existing one changes status — treat "zero open warnings" as a release gate for M13.

Corresponding GitHub issue: see the repo's Issues tab (label `warnings`) — file/update one per **open** row below so the tracking isn't only local to this doc.

## Current status (as of the M1 verify-and-fix checkpoint build, `checkpoint.log`)

| # | Warning | Location | Status | Notes |
|---|---|---|---|---|
| 1 | `warning: extension declares a conformance of imported type 'X' to imported protocol 'Content'; this will not behave correctly if the owners of 'X' introduce a conformance of their own` (×15 — one per DTO) | `Server/Sources/App/Extensions/ContentConformance.swift` | **fixed** | Both `Content` (Vapor) and every DTO (`ExpatEventsAPI`) are external to this module, so Swift flags it as a "retroactive conformance." This is intentional — the shared package is deliberately Foundation-only and can't declare `Content` itself (see the file's own doc comment) — so each extension is annotated `@retroactive` to acknowledge it rather than suppress the underlying check. Confirmed gone from the checkpoint build's output. Low risk in practice: we own both sides of the build (no third-party package will ever add its own `Content` conformance to our DTOs), but keep this row if a future Swift version changes `@retroactive`'s semantics. |
| 2 | `warning: 'postgres(hostname:port:username:password:database:tlsConfiguration:maxConnectionsPerEventLoop:connectionPoolTimeout:encoder:decoder:sqlLogLevel:)' is deprecated: Use '.postgres(configuration:maxConnectionsPerEventLoop:connectionPoolTimeout:encodingContext:decodingContext:sqlLogLevel:)' instead.` (now confirmed via a build where `configure.swift` actually recompiled — see row 4 of the earlier build's false-negative, which is what this exact text corrects) | `Server/Sources/App/configure.swift:36`, the `app.databases.use(.postgres(hostname:...))` fallback branch used for local dev (no `DATABASE_URL`) | **open** | Tracked in [issue #1](https://github.com/heathdj/expat-events-vapor/issues/1) — will update that issue with this exact text too. The replacement takes a single `configuration:` parameter instead of the individual `hostname`/`port`/etc. labels, almost certainly a `SQLPostgresConfiguration` (PostgresKit) built separately, e.g. `SQLPostgresConfiguration(hostname:port:username:password:database:tls:)` then `.postgres(configuration: that)`. Not attempting this blind: the `tls:` parameter's exact type/default and whether `SQLPostgresConfiguration` needs an explicit extra import (vs. being re-exported transitively through `FluentPostgresDriver`) aren't things I can confirm without a real compiler, and this is a dev-only fallback path (production and any real deployment use the `DATABASE_URL` branch above it, untouched by this warning) — not worth another user round-trip to chase down mid-M1. Revisit with a real build attempt at or before M13. |
| 3 | `warning: 'jwt': found 1 file(s) which are unhandled; explicitly declare them as resources or exclude from the target` — `.../jwt/Sources/JWT/Docs.docc` | Emitted by SwiftPM for the `jwt` (JWTKit) dependency itself, not our code — appears once per `swift build` and once per `swift test` invocation | **suppressed** | Third-party package resource warning: the pinned JWTKit release ships a DocC catalog its own `Package.swift` doesn't declare as a resource/exclusion. We don't vendor or patch dependencies, so there's nothing to change on our side short of pinning a different JWTKit version — not worth doing for a cosmetic SwiftPM warning. Revisit only if upgrading `jwt` (see `Server/Package.swift`) happens to pick up an upstream fix, or if this starts obscuring real warnings in the log. |
| 4 | `warning: instance method 'test' is unavailable from asynchronous contexts; Use the async method instead.; this is an error in the Swift 6 language mode` + `warning: no 'async' operations occur within 'await' expression` (×2, one pair per call site — currently 1 call site, `testHealthCheckReturns200`) | `Server/Tests/AppTests/AppTests.swift:30`, the `app.testable().test(.GET, "health") { ... }` call | **open** | `XCTVapor`'s `test(...)` has both a plain-`throws` overload (marked `noasync` — deliberately unavailable from `async` contexts) and presumably an `async throws` overload; since our test function is `async` and the closure we pass isn't itself marked `async`, overload resolution is landing on the sync one, which is why it warns instead of erroring outright. Needs the exact async-overload signature confirmed against a real compiler (not guessed at) before changing the call — likely just marking the trailing closure `async` (`{ res async in ... }`), but unverified. Explicitly called out as **"this is an error in the Swift 6 language mode"** — i.e. this stops being a warning and starts being a build break the moment this package adopts Swift 6 language mode (via `swiftLanguageMode` in `Package.swift`), so it's higher-priority than a typical cosmetic warning even though nothing forces that mode today. Will recur at every additional `app.testable().test(...)` call site added in later milestones' tests until fixed once at the root cause. |

## Process

1. Every time a milestone branch produces a build log, grep it for `warning:` and reconcile against this table — add new rows, update statuses, don't just glance at the tail (a warning count can hide behind others; always check the total, e.g. `grep -c "warning:" build.log`, and confirm which lines it maps to before assuming it's "just the one you already know about" — that's exactly the mistake this table exists to catch).
2. **Incremental builds hide warnings from files that didn't change** (`swift build` only re-emits diagnostics for files it recompiles) — a warning missing from one log is not evidence it's fixed unless the fix actually touched that file, or the build was clean. Before trusting a "no warnings" log as proof a row is resolved, either confirm the relevant file was recompiled in that log, or run a clean build (`swift package clean && swift build`) once per milestone checkpoint.
3. Each **open** row gets (or keeps updated) a matching GitHub issue labeled `warnings`, so it's visible outside this repo's working tree too and can be assigned/triaged independently of any one milestone's PR.
4. Before M13 (deployment), every row must be **fixed** or **suppressed with a written reason** — no **open** rows at launch.
